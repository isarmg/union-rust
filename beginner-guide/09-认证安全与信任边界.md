# 09. 认证与信任边界

外层反代终止 TLS 并只连接 Union。Union 清洗伪造内部头，按固定 audience/prefix 注入一次性
gateway token。worker 逐请求验证并在健康响应回显身份。

gateway token 不代表终端用户。管理接口仍需 Union 会话/CSRF；Agent 仍需设备凭据；Photo、
Sentinel、Dufs 的域权限仍由各模块实现。直接访问回环 worker 不受支持。
