# 00. 学习准备

需要 Rust、Cargo、Node/npm、Git 和 PostgreSQL。Dufs 额外使用模块私有 SQLite；Sentinel 可
需要 MediaMTX。先运行只读检查：

```bash
cargo metadata --no-deps
cargo test -p sarmg-protocol
cd web && npm ci && npm test
```

完整服务端依赖发行布局和模块环境，优先使用 union-builder 的调试 profile，不要把单个
worker 的开发启动称为部署。
