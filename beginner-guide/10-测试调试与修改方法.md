# 10. 测试与调试

改动应覆盖三层：单 crate 测试、gateway/supervisor 合同测试、Builder profile/layout 测试。
重点验证未选模块缺席、内部头伪造被拒、健康身份不匹配失败、崩溃退避与 SIGTERM。

数据库测试使用隔离 PostgreSQL schema 或 Dufs 临时 SQLite；迁移测试只读取旧库副本。测试
通过不等于生产验收，后者必须记录目标平台、release id、profile 和实际输出。
