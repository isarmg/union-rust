# Archived

This repository is being retired as the Union runtime. Its reusable auth, HTTP, session,
CSRF and request-id pieces should be extracted into `isarmg-foundation`; the remaining
Union Core/Web Shell is archived and must not be extended.
